diskscan {
  db_path = "test.db"
  diffdb_path = "test_diff.db"
  output_kind = file
  output_path = "test.output"

  directory "test_dir" {
    realtime = true
    recursive = true
    checkdiff = true
  }
}
